{{#-bs/row class="justify-content-center"}}
  {{#-bs/col sm=12 md=10 lg=8}}
    <h2>
      <img src="{{user.activeOrganization.photoURL}}" class="icon icon-lg mr-4" alt={{user.activeOrganization.name}}>
      {{user.activeOrganization.name}}
    </h2>

  {{/-bs/col}}
{{/-bs/row}}

<hr>
