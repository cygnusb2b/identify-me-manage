  <li class="nav-item dropdown">
    <a class="nav-link dropdown-toggle" data-toggle="dropdown" href="javascript:void(0)" role="button">
      <img src={{orgManager.activeOrg.photoURL}} class="avatar mr-1" height="20" width="20">
      {{orgManager.activeOrg.name}}
      </a>

    <div class="dropdown-menu dropdown-menu-right">
      {{#if orgManager.canManageOrg}}
        {{#link-to 'app.organization.settings' orgManager.activeOrgId title='Manage' class='dropdown-item'}}
          {{-ion 'ios-gear'}} Manage Organization
        {{/link-to}}
        <div class="dropdown-divider"></div>
      {{/if}}

      {{#link-to 'app.organization-create' title='New' class='dropdown-item'}}
        {{-ion 'plus'}} New Organization
      {{/link-to}}

      <div class="dropdown-divider"></div>
      
      {{#each orgManager.availableOrgs as |org|}}
        {{!-- <a href='#' {{action 'switchOrg' org.id}} class='dropdown-item'> --}}
         {{#link-to 'app.organization' org.id class='dropdown-item'}} 
          <img src={{org.photoURL}} class="avatar mr-1" height="20" width="20">
          {{org.name}}
         {{/link-to}} 
        {{!-- </a> --}}
      {{/each}}
    </div>
  </li>
