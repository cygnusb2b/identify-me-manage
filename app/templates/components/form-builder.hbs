{{yield (hash
  fieldsGlobal=fieldsGlobal
  fieldTypes=fieldTypes
  fieldsSorted=fieldsSorted
  selectedField=selectedField
  hasSelectedField=hasSelectedField
  save=(action "save")
  createField=(action "createField")
  assignField=(action "assignField")
  selectField=(action "selectField")
  applyFieldValues=(action "applyFieldValues")
  moveField=(action "moveField")
  removeField=(action "removeField")
  reorderFields=(action "reorderFields")
)
}}

