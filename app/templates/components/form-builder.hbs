{{yield (hash
  fieldTypes=fieldTypes
  fields=fields
  selected=selected
  hasSelectedField=hasSelectedField
  addField=(action "addField")
  moveField=(action "moveField")
  removeField=(action "removeField")
  selectField=(action "selectField")
  reorderFields=(action "reorderFields")
)
}}

