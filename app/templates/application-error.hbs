{{#-bs/row class="justify-content-center"}}
  {{#-bs/col sm=10 md=8 lg=6}}
   <div class="alert alert-warning" role="alert">
    <h4 class="alert-heading">{{-ion "alert-circled"}} Houston, we have a problem!</h4>
    <p>An error was encountered with this request:</p>
    <p class="mb-0">{{ model.message }}</p>
  </div>
  {{/-bs/col}}
{{/-bs/row}}
